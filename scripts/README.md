# Проверка в браузере

Два скрипта на Playwright. В зависимости проекта Playwright намеренно **не добавлен** —
он нужен только для ручной проверки и утяжелил бы установку.

```bash
# один раз
npm install -g playwright && npx playwright install --with-deps chromium

# приложение должно быть запущено
pnpm build && pnpm start

# проверки (BASE_URL по умолчанию http://localhost:3000)
node scripts/visual-check.mjs   # 17 страниц × 2 брейкпоинта
node scripts/flow-check.mjs     # 27 интерактивных сценариев
```

**`visual-check.mjs`** снимает скриншоты в `.screenshots/` и проверяет на каждой странице:
горизонтальную прокрутку страницы (с указанием элемента-виновника), ошибки консоли,
расхождения гидратации и коды ответа.

**`flow-check.mjs`** проходит сценарии: мега-меню, поиск по Ctrl+K с автокомплитом,
фасет → URL → кнопка «назад», сравнение через панель, добавление в корзину без ухода
со страницы, живучесть корзины после перезагрузки, валидация чекаута и автоформат
телефона, квиз подбора до непустого результата, мобильное меню и панель фильтров.

Оба скрипта завершаются списком проверок; `flow-check.mjs` печатает итог вида `27 из 27`.
